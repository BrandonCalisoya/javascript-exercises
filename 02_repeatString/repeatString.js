const repeatString = function(string, num) {
    let repeatWord="";
    if(num>=0){
        
    for (let i = 0; i < num; i++) {
        repeatWord = repeatWord + string;
    }
    return repeatWord;
    }{
        return repetaWord = "ERROR";
    }
};

// Do not edit below this line
module.exports = repeatString;
